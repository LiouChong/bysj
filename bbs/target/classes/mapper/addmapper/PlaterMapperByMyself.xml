<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.extend.PlaterMapperExtends">
    <select id="booleanPlater" resultType="Integer">
        select plate_id from plater where user_id = #{userId}
    </select>

</mapper>